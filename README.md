# Customer_Segmentation
Hybrid segmentation pipeline combining Unsupervised Learning (K-Means) to discover customer groups 
(labels) and Supervised Learning (Random Forest) to classify new customers into established groups
